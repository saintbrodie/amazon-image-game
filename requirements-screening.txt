# Optional dependencies for local image privacy/safety screening.
# The core game and normal CI do not require these packages.
Pillow>=10.0
# screen_dataset.py uses the OpenCV 4 CascadeClassifier API.
opencv-python-headless>=4.10,<5
